{
  "name": "@fireweaveai/fireweave-cloud-bridge",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Stdio MCP shim that bridges Claude Code to fw-server's REST API. Uses the active fw-cli profile's bearer token. Replaces fireweave-server-proxy's discovery-based wiring with a generated MCP↔REST translator (T2/T3) and a startup whoami probe (T4).",
  "bin": {
    "bridge": "src/server.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "^1.3.11"
  }
}
